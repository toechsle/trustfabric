import { EqualValidatorClientsDirective } from './equal-validator-clients.directive';

describe('EqualValidatorClientsDirective', () => {
  it('should create an instance', () => {
    const directive = new EqualValidatorClientsDirective();
    expect(directive).toBeTruthy();
  });
});
