import { EqualValidatorAdminConsultantDirective } from './equal-validator-admin-consultant.directive';

describe('EqualValidatorAdminConsultantDirective', () => {
  it('should create an instance', () => {
    const directive = new EqualValidatorAdminConsultantDirective();
    expect(directive).toBeTruthy();
  });
});
