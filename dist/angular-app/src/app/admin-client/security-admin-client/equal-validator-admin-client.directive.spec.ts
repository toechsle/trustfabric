import { EqualValidatorAdminClientDirective } from './equal-validator-admin-client.directive';

describe('EqualValidatorAdminClientDirective', () => {
  it('should create an instance', () => {
    const directive = new EqualValidatorAdminClientDirective();
    expect(directive).toBeTruthy();
  });
});
